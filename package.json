{
    "name": "Bar",
    "version": "2.03.00",
    "description": "Bar",
    "scripts": {
        "start": "npm run start --prefix ../",
        "build": "npm run build --prefix ../",
        "create-project": "git checkout --orphan"
    },
    "dependencies": {
        "file-saver": "^2.0.2",
        "xlsx": "^0.14.3"
    },
    "devDependencies": {
        "script-loader": "^0.7.2"
    }
}
