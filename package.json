{
    "name": "workspace-master",
    "version": "1.0.0",
    "description": "Workspace Master",
    "scripts": {
        "start": "npm run start --prefix ../",
        "build": "npm run build --prefix ../",
        "create-project": "git checkout --orphan"
    },
    "dependencies": {
        "jssha": "^2.3.1",
        "vue-loading-overlay": "^3.2.0"
    }
}
